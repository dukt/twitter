{% import "_includes/forms" as forms %}

{{ forms.textField({
    first: true,
    label: "Search Query"|t('twitter'),
    instructions: "Enter keywords as well as AND, OR operators"|t('twitter'),
    name: 'query',
    value: (settings.query is not null ? settings.query : '')
}) }}

{{ forms.textField({
    first: true,
    label: "Count"|t('twitter'),
    instructions: "Number of tweets that will be displayed by the widget"|t('twitter'),
    name: 'count',
    size: 4,
    value: (settings.count is not null ? settings.count : 10)
}) }}
